@for (var i = 0; i < (StoryLines?.Count ?? 0); i++)
{
    <ReqRespActor
        @key="i"
        ActorId="@i"
        StoryLine="@StoryLines![i]"
        StoryLineFinished="@OnActorStoryLineCompleted">
    </ReqRespActor>
}
